// Coach - Global Application Configuration
export const GLOBAL_CONFIG = {
  whatsapp: {
    // Set to true and fill the credentials below to enable background WhatsApp messages globally for all teachers.
    // If set to false or left blank, the app will automatically fall back to manual browser tab redirects (wa.me).
    enabled: false,
    idInstance: '', 
    apiTokenInstance: '' 
  }
};
